describe('pitch', function(){

    it("returns 1 for an entry of ", function(){
        expect(pitch(0)).not.to.equal(-1);
    });

    it("returns 1 for an entry of ", function(){
        expect(pitch(0)).not.to.equal(-1);
    });



});
